{
  "name": "cookpropertytax",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:web",
    "build:web": "esbuild src/web/app.ts --bundle --format=esm --target=es2022 --minify --outfile=public/app.js",
    "dev": "wrangler dev",
    "format": "biome check --write src public docs package.json tsconfig.json vitest.config.ts wrangler.jsonc biome.json",
    "lint": "biome check src public docs package.json tsconfig.json vitest.config.ts wrangler.jsonc biome.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "verify": "npm run build && npm run lint && npm run typecheck && npm test"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@cloudflare/workers-types": "^4.20250707.0",
    "@types/node": "^22.10.7",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.101.0"
  }
}
